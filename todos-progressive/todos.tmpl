<!DOCTYPE html>
<html>
<head>
    <title>Progressive Complexity: Todo Demo</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css">
</head>
<body>
    <main class="container">
        <hgroup>
            <h1>Todos ({{.ActiveCount}} remaining)</h1>
            <p>Zero <code>lvt-*</code> attributes &mdash; standard HTML only</p>
        </hgroup>

        <!-- Auto-submit form: routes to Submit() -->
        <fieldset role="group">
            <input type="text" name="Title" placeholder="What needs to be done?" required
                   form="add-form" {{if .lvt.HasError "title"}}aria-invalid="true"{{end}}>
            <button type="submit" name="submit" form="add-form">Add</button>
        </fieldset>
        <form method="POST" id="add-form"></form>
        {{if .lvt.HasError "title"}}
            <small>{{.lvt.Error "title"}}</small>
        {{end}}

        <!-- Button name routing: button name="toggle" → Toggle(), name="delete" → Delete() -->
        <table>
            <tbody>
            {{range .FilteredItems}}
                <tr data-key="{{.ID}}">
                    <td>
                        <form method="POST">
                            <input type="hidden" name="id" value="{{.ID}}">
                            {{if .Done}}<s>{{.Title}}</s>{{else}}{{.Title}}{{end}}
                        </form>
                    </td>
                    <td>
                        <form method="POST">
                            <input type="hidden" name="id" value="{{.ID}}">
                            <button type="submit" name="toggle" class="secondary outline">
                                {{if .Done}}Undo{{else}}Done{{end}}
                            </button>
                        </form>
                    </td>
                    <td>
                        <form method="POST">
                            <input type="hidden" name="id" value="{{.ID}}">
                            <button type="submit" name="delete" class="contrast outline">Delete</button>
                        </form>
                    </td>
                </tr>
            {{end}}
            </tbody>
        </table>

        <!-- Button name routing: button name="filter" → Filter() -->
        <fieldset role="group">
            <form method="POST"><input type="hidden" name="filter" value="all"><button type="submit" name="filter" class="secondary outline">All</button></form>
            <form method="POST"><input type="hidden" name="filter" value="active"><button type="submit" name="filter" class="secondary outline">Active</button></form>
            <form method="POST"><input type="hidden" name="filter" value="done"><button type="submit" name="filter" class="secondary outline">Done</button></form>
        </fieldset>
    </main>

    {{if .lvt.DevMode}}
    <script src="/livetemplate-client.js"></script>
    {{else}}
    <script src="https://cdn.jsdelivr.net/npm/@livetemplate/client@latest/dist/livetemplate-client.browser.js"></script>
    {{end}}
</body>
</html>
